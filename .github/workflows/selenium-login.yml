name: Selenium Login E2E

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      chrome:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444

    env:
      VITE_SUPABASE_URL: 'https://rwvshowhpvcfxdbsapmi.supabase.co'
      VITE_SUPABASE_PUBLISHABLE_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ3dnNob3docHZjZnhkYnNhcG1pIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODAyMzc3MTQsImV4cCI6MjA5NTgxMzcxNH0.KnQVTGMVoQFuqpReGhMBCTr07iF-1hHwYlkJq-rXeMQ'
      TEST_USER_EMAIL: 'gayathrikalluri17@gmail.com'
      TEST_USER_PASSWORD: 'daa@2004'
      SELENIUM_SERVER_URL: 'http://localhost:4444'
      CI: 'true'
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install dependencies
        run: npm install --force
      - name: Build and start preview server
        run: |
          npx vite build
          npx vite preview --port 4173 &
          npx wait-on http://127.0.0.1:4173 --timeout 30000
      - name: Run Selenium tests
        env:
          TEST_URL: 'http://127.0.0.1:4173'
        run: npx mocha "selenium-tests/tests/**/*.test.js" --timeout 120000
